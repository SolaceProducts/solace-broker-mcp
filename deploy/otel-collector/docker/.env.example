# Default backend: Grafana Tempo.
# To switch to Jaeger: docker compose --env-file .env.jaeger up
COMPOSE_PROFILES=tempo
OTEL_BACKEND_ENDPOINT=tempo:4317

# Dev token for the static auth mode. Change this before exposing the server
# on any network beyond your local machine.
MCP_DEV_TOKEN=change-me
