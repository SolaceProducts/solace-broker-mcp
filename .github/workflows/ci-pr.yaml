name: CI - Pull Request

on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read
  pull-requests: write
  id-token: write
  actions: read
  issues: write
  checks: write
  statuses: write

jobs:
  fossa_scan:
    name: FOSSA Scan
    uses: SolaceDev/solace-public-workflows/.github/workflows/sca-scan-and-guard.yaml@fc521b087f780457f92d4cb46038184e92ad2fbc # pinned 2026-05-12
    with:
      use_vault: true
      config_file: '.github/workflow-config.json'
    secrets:
      # Vault address is injected from an org/repo secret, not committed to the repo.
      VAULT_URL: ${{ secrets.VAULT_URL }}

  # The DCO gate itself runs from .github/workflows/dco.yaml on
  # pull_request_target, which means it executes the *base ref's* script — so a
  # PR that edits dco-check.sh never exercises its own version there. This job
  # closes that gap: it is an ordinary `pull_request` test of the PR's copy. It
  # enforces nothing (the gate does that); it catches a regression before merge.
  dco_selftest:
    name: DCO check self-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run the DCO check's self-test against this PR's version
        run: .github/scripts/dco-check.test.sh

  changelog:
    name: CHANGELOG updated
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Advise on a missing [Unreleased] entry for behavior-changing PRs
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          LABELS: ${{ join(github.event.pull_request.labels.*.name, ',') }}
          # advisory = warn but don't block the PR; the release is the hard gate.
          # Flip to "blocking" to fail PRs that skip the changelog.
          CHANGELOG_GATE_MODE: advisory
        run: .github/scripts/changelog-check.sh
